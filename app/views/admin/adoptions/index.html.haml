= render :partial => '/adoptions/table', :locals => { :adoptions => @adoptions }
%br/

%h2 Total ducks adopted: #{@total_ducks}
%br/
%h2 Total donations raised: #{number_to_currency dollar_fee(@total_donations)}

%br/
%br/

- content_for :navlinks do
  .navlink= link_to "Admin Dashboard", admin_root_path
