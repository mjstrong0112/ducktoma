require 'spec_helper'

describe PaymentNotificationsController do
  it "renders nothing on create"# do
  #  post :create, {:email => "testemail@provider.com"}
  #  response.body.should be_blank
  #end
  it "creates a payment notification when sent valid information"
  it "doesn't create a payment notification when sent invalid information"
end
